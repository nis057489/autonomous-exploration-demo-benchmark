build/
maps/*.grid
maps/*.vxch
maps/state.json
!maps/.gitkeep
figures/*.png
!figures/.gitkeep
__pycache__/

# web/site/vxch.js + vxch.wasm are emcc build output (see web/CMakeLists.txt) --
# regenerated by building web/, not hand-authored like the rest of web/site/.
web/site/vxch.js
web/site/vxch.wasm

# Datasets and generated archives: new_college.graph alone is ~393 MB, and the
# .vxch archives are reproducible from it with graph2vxch (see README).
datasets/*.graph
datasets/*.vxch
